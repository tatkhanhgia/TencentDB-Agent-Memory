# Add under [mcp_servers.tdai-memory] in ~/.codex/config.toml
# Inherit secrets from the environment — do not paste keys here.

[mcp_servers.tdai-memory]
command = "tdai-memory-mcp"

[mcp_servers.tdai-memory.env]
TDAI_ENDPOINT = "http://127.0.0.1:8420"
TDAI_SERVICE_ID = "default"
