# Add under [mcp_servers.tdai-memory] in ~/.codex/config.toml
# Stdio is the standard transport for every harness; do not use HTTP/remote MCP here.
# The wrapper sources env internally — no env table.

[mcp_servers.tdai-memory]
command = "/Users/mac/Documents/Projects/MyProjects/TencentDB-Agent-Memory/deploy/global-images/tdai-memory-mcp.sh"
